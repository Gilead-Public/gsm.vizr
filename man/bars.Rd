% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bars.R
\name{bars}
\alias{bars}
\title{Render a gsm.viz bars chart}
\usage{
bars(
  data,
  spec,
  metadata = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  minHeight = 500,
  bDebug = FALSE
)
}
\arguments{
\item{data}{\code{data.frame} Long rows; one row per observation (\code{stat = "count"})
or one row per category/segment (\code{stat = "identity"}).}

\item{spec}{\code{list} Chart specification, typically from \code{bars_spec()}.}

\item{metadata}{\code{list} Named list of report keys (e.g. \code{chartId}) echoed in
event payloads. Not a data.frame — event consumers expect a JSON object.}

\item{width, height, elementId}{Standard htmlwidgets arguments.}

\item{minHeight}{\code{numeric} Minimum widget height in pixels; applied as a
CSS floor so \code{theme$dynamicSizing} cannot collapse small charts. Default 500.}

\item{bDebug}{\code{logical} Log the payload to the browser console.}
}
\value{
A \code{bars} htmlwidget.
}
\description{
Thin htmlwidget over \code{gsmViz.default.bars}. \code{spec} maps 1:1 to the
JS spec (see \code{bars_spec()}); gsm.viz's documentation is the source of
truth for its keys.
}

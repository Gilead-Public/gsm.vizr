% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxy.R
\name{bars_proxy}
\alias{bars_proxy}
\alias{proxy_update_data}
\alias{proxy_update_spec}
\alias{proxy_select_category}
\alias{proxy_select_segment}
\alias{proxy_clear_selection}
\alias{proxy_export_image}
\title{Proxy for server-side updates to a rendered bars widget}
\usage{
bars_proxy(id, session = shiny::getDefaultReactiveDomain())

proxy_update_data(proxy, data, spec = NULL)

proxy_update_spec(proxy, spec)

proxy_select_category(proxy, values, silent = TRUE)

proxy_select_segment(proxy, values, silent = TRUE)

proxy_clear_selection(proxy, silent = TRUE)

proxy_export_image(proxy, filename = NULL)
}
\arguments{
\item{id}{\code{character(1)} The widget's Shiny output id.}

\item{session}{The Shiny session.}

\item{proxy}{A \code{gsm_vizr_proxy} from \code{\link[=bars_proxy]{bars_proxy()}}.}

\item{data, spec, values, silent, filename}{Verb arguments; see Details.}
}
\value{
A \code{gsm_vizr_proxy}; verbs return it invisibly for piping.
}
\description{
Verbs map 1:1 to \code{chart.helpers}. Selection verbs pass \verb{_silent} by
default so server-driven updates do not echo select events back to the
server (no feedback loops). \code{proxy_update_data()} without a spec reuses
the live browser-side spec (\code{chart.data._spec_}) — the R side never
re-pushes a cached spec over toggle-mutated state.
}
\details{
Proxy verbs target \code{\link[=bars]{bars()}} widgets only in 0.1.0. A verb aimed at a
\code{\link[=facet_bars]{facet_bars()}} widget raises a browser-side error naming the widget and the
verb rather than silently doing nothing.
}

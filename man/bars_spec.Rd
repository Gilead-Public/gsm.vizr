% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bars_spec.R
\name{bars_spec}
\alias{bars_spec}
\title{Build and validate a gsm.viz bars spec}
\usage{
bars_spec(
  x,
  y = NULL,
  fill = NULL,
  orientation = "vertical",
  position = "stack",
  stat = NULL,
  nCategories = NULL,
  scales = NULL,
  labels = NULL,
  annotations = NULL,
  tooltip = NULL,
  callbacks = NULL,
  selection = NULL,
  theme = NULL,
  zoom = NULL,
  legend = NULL,
  interactive = TRUE
)
}
\arguments{
\item{x, y, fill}{\code{character(1)} Column names for the aesthetics; \code{x} required.}

\item{orientation}{\code{"vertical"} or \code{"horizontal"}.}

\item{position}{One of \code{"stack"}, \code{"dodge"}, \code{"identity"}, \code{"fill"}, \code{"layer"}.}

\item{stat}{One of \code{"count"}, \code{"identity"}, \code{"percent"} (gsm.viz default: count).}

\item{nCategories}{\code{integer(1)} Top-N category cap.}

\item{scales, labels, annotations, tooltip, callbacks, selection, theme, zoom, legend}{\code{list} Passed through 1:1; see the gsm.viz bars documentation.}

\item{interactive}{\code{logical(1)} gsm.viz \code{interactive} flag.}
}
\value{
A plain list ready for \code{\link[=bars]{bars()}}.
}
\description{
Faithful 1:1 construction of the JS spec object: arguments map to
top-level spec keys, aesthetics nest under \code{mapping}. Validation mirrors
gsm.viz \code{src/bars/validateSpec.js} (same messages), so invalid specs fail
in R before reaching the browser. JSON-safe features (\code{tooltip$format},
label templates) are the documented first choice; \code{js_hook()} is the
escape hatch for function-valued slots.
}
